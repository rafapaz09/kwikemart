@model List<Kwikemart_APP.Models.ProductsData>
@if(Model != null && Model.Count > 0)
{
    @foreach(var item in Model)
    {
        <tr>
            <td>@item.Name</td>
            <td>@item.ProductPrice</td>
            <td>@item.Popularity</td>
        </tr>
    }
}